name: Consolidate 3DLite Production (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Production is already consolidated into index.html; no legacy patch-chain rebuild is required."
