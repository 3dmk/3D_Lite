# 3D_Lite

Current GitHub Pages development build: **v3.99.7 — GI Controls Update**

The visible editor version is owned by `ThreeDLiteVersion.version`.

Public version surfaces synchronized at startup:
- browser title
- top-right editor version label
- renderer diagnostics version
- `data-app-version`
- `window.__3DLiteAppVersion`
- live-build metadata

Current GI defaults:
- Primary GI: Irradiance
- Secondary GI: Light Cache
- Brute Force removed from the GI settings UI
- GI controls enabled

Historical subsystem version strings are intentionally preserved.
