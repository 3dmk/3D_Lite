# GitHub Pages L3N workflow

- `index.html` is the live GitHub Pages entry point.
- Run the L3N runtime test from the browser.
- Copy the JSON report directly from the editor.
- Feed that report into the next L3N cycle.
- Do not promote a build with startup/viewport/runtime regressions.
