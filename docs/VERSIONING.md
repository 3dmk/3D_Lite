# 3DLite version ownership

Current app build: **v3.94.1**

`ThreeDLiteVersion.version` is the single source for the visible/public editor version.

Do not replace historical subsystem version fields just to match the current app build; they describe the subsystem implementation lineage.
