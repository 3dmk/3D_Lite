(function(g){'use strict';
function classify(e){const p=e?.performance||{},s=p.stageMs||{},total=Math.max(1,+e?.elapsedMs||1),rays=Math.max(1,+p.totalRays||1),mem=+e?.pathStats?.memory?.usedBytes||0;const shares={render:(+s.render||0)/total,worker:(+s.worker||0)/total,bvh:(+s.bvh||0)/total};const candidates=[{id:'cpu-render-loop',domain:'CPU',score:shares.render,evidence:{renderShare:shares.render}},{id:'worker-preparation',domain:'CPU/Workers',score:shares.worker,evidence:{workerShare:shares.worker}},{id:'bvh-traversal',domain:'BVH',score:shares.bvh,evidence:{bvhShare:shares.bvh}},{id:'path-memory',domain:'Memory',score:Math.min(1,mem/268435456),evidence:{usedBytes:mem}},{id:'ray-efficiency',domain:'Rays',score:(+p.pathRays||0)/rays,evidence:{pathRayShare:(+p.pathRays||0)/rays}}];candidates.sort((a,b)=>b.score-a.score||a.id.localeCompare(b.id));return Object.freeze({shares:Object.freeze(shares),candidates:Object.freeze(candidates),selected:candidates[0]});}
function plan(e){const a=classify(e),s=a.selected;return Object.freeze({analysis:a,experiment:Object.freeze({id:'block-e-'+s.id,domain:s.domain,goal:s.id==='cpu-render-loop'?'reduce render-loop cost without changing framebuffer quality':'reduce '+s.id+' cost without quality regression',requiresSameWorkload:true,requiresQualityGate:true,requiresRegressionGate:true})});}
g.L3NLitePixSpecialization=Object.freeze({version:'0.1.0',classify,plan});
})(typeof globalThis!=='undefined'?globalThis:window);
