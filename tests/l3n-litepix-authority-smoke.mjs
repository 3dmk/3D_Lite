import fs from 'node:fs';import vm from 'node:vm';const c={};c.globalThis=c;vm.createContext(c);vm.runInContext(fs.readFileSync('l3n-litepix/authority/authority.js','utf8'),c);const A=c.L3NLitePixAuthority;
const x=A.candidate({baselineRevision:'base1',candidateRevision:'cand1',files:['b.js','a.js','a.js'],intent:'optimize'});const good=A.evidence({candidate:x,source:true,readBack:true,build:true,launch:true,render:true,quality:true,regression:true,artifactFingerprint:'abc'});const bad=A.evidence({candidate:x,source:true,readBack:true,build:true,launch:true,render:true,quality:false,regression:true,artifactFingerprint:'abc'});if(!A.decide(x,good).promote||A.decide(x,bad).promote)throw Error('authority gate');let rejected=false;try{A.candidate({baselineRevision:'same',candidateRevision:'same'})}catch{rejected=true}if(!rejected)throw Error('lineage gate');console.log(JSON.stringify({passed:true,version:A.version,candidate:x,good:A.decide(x,good),bad:A.decide(x,bad)},null,2));
