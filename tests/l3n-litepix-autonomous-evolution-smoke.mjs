import fs from 'node:fs';import vm from 'node:vm';const c={console,Date};c.globalThis=c;vm.createContext(c);for(const f of ['authority/authority.js','authority/candidate-controller.js','evolution/planner.js','evolution/knowledge.js','evolution/autonomous-loop.js'])vm.runInContext(fs.readFileSync('l3n-litepix/'+f,'utf8'),c,{filename:f});const baseline=JSON.parse(fs.readFileSync('l3n-litepix/evidence/production-render-baseline-v1.json','utf8'));baseline.revision='62e1ccb';const P=c.L3NLitePixEvolutionPlanner,a=P.analyze(baseline),h=P.hypothesis(a);if(a.selected?.id!=='benchmark-visibility-gap'||h?.id!=='repair-visible-reference-scene')throw Error('autonomous selection');const good={source:true,readBack:true,build:true,launch:true,render:true,quality:true,regression:true,artifactFingerprint:'candidate-frame'};const ok=await c.L3NLitePixAutonomousEvolution.evolve({baseline,candidateRevision:'candidate-good',files:['benchmark-scene'],implement:async()=>({changed:true}),verify:async()=>good});if(ok.status!=='SUCCESS')throw Error('success flow');const bad=await c.L3NLitePixAutonomousEvolution.evolve({baseline,candidateRevision:'candidate-bad',files:['litepix/path'],implement:async()=>({changed:true}),verify:async()=>({...good,quality:false})});if(bad.status!=='FAILED_RECOVERED'||bad.recovery.status!=='RECOVERED')throw Error('reject/recover flow');console.log(JSON.stringify({passed:true,selected:a.selected,hypothesis:h,success:ok.status,rejection:bad.status,recovery:bad.recovery.status,lessons:bad.knowledge.length},null,2));
