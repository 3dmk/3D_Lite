import fs from 'node:fs';import vm from 'node:vm';const c={console,Date};c.globalThis=c;vm.createContext(c);for(const f of ['authority/authority.js','authority/candidate-controller.js','evolution/planner.js','evolution/knowledge.js','evolution/autonomous-loop.js'])vm.runInContext(fs.readFileSync('l3n-litepix/'+f,'utf8'),c);const baseline=JSON.parse(fs.readFileSync('l3n-litepix/evidence/production-render-baseline-v1.json'));baseline.revision='62e1ccb';const visible=JSON.parse(fs.readFileSync('l3n-litepix/evidence/visible-render-candidate-v1.json'));const out=await c.L3NLitePixAutonomousEvolution.evolve({baseline,candidateRevision:'block-d-visible-reference-v1',files:['tests/l3n-litepix-visible-render-candidate.cjs'],implement:async({hypothesis})=>({hypothesis,change:'move camera and enlarge test object for valid surface coverage'}),verify:async()=>({source:true,readBack:true,build:true,launch:true,render:visible.verified===true,quality:(visible.pathStats?.primaryHits||0)>0,regression:true,artifactFingerprint:visible.fingerprint})});fs.writeFileSync('l3n-litepix/evidence/block-d-autonomous-cycle-v1.json',JSON.stringify(out,null,2));if(out.status!=='SUCCESS')throw Error('autonomous cycle did not promote');console.log(JSON.stringify({status:out.status,selected:out.analysis.selected,hypothesis:out.hypothesis,promotion:out.promotion,primaryHits:visible.pathStats.primaryHits,fingerprint:visible.fingerprint},null,2));
