import fs from 'node:fs';import vm from 'node:vm';const ctx={navigator:{hardwareConcurrency:16,deviceMemory:8,gpu:{}},performance:{memory:{jsHeapSizeLimit:1000,usedJSHeapSize:250},now:(()=>{let t=0;return()=>++t;})()}};ctx.globalThis=ctx;vm.createContext(ctx);for(const f of ['evidence-collector.js','renderer-instrumentation.js','runtime-evidence.js'])vm.runInContext(fs.readFileSync(new URL('../l3n-litepix/'+f,import.meta.url),'utf8'),ctx);const path={record(){},completePass(){},snapshot(){return{version:'4.10.0',mode:'test',elapsedMs:12,memory:{used:64},gi:{active:2},guide:{writes:4},spatial:{reuse:1}}}};const R=ctx.L3NLitePixRuntimeEvidence;if(R.instrumentPath(path)!==3)throw new Error('path wrapping');path.record();path.record();path.completePass();R.capturePath(path);const h=R.captureHardware(),s=ctx.L3NLitePixEvidence.snapshot();if(s.counters.pathSamples!==2||s.counters.pathPasses!==1||h.logicalProcessors!==16||!h.webgpu||s.meta.pathMemory.used!==64)throw new Error('runtime evidence');console.log(JSON.stringify({passed:true,counters:s.counters,hardware:h,path:{version:s.meta.pathVersion,memory:s.meta.pathMemory}},null,2));
